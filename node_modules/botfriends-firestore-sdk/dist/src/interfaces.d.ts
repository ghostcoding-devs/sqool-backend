import { firestore, ServiceAccount } from 'firebase-admin';
export interface ExtendedServiceAccount {
    serviceAccount: ServiceAccount;
    identifier: string;
}
export interface FilterOptions {
    fieldPath: string | firestore.FieldPath;
    opStr: any;
    value: any;
}
export interface ExtendedDocument extends firestore.DocumentData {
    id: string;
}
